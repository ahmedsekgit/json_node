var express = require('express');
var tsController = require('../controllers/ts-controller');
var router = express.Router();
// to display data 
/*
router.get('/read', tsController.readData);
router.get('/store', tsController.storeData);
router.get('/check', tsController.checkData);
*/
module.exports = router;